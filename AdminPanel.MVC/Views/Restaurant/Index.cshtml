@model IEnumerable<AdminPanel.Common.Models.Restaurant.Restaurant>

<div class="mt-2" style="width:50%">
    <div class="row">
        <h2>Restaurants</h2>
        <div class="col-md-4">
            <a href="/Restaurant/Create" class="btn btn-primary">Add Restaurant</a>
        </div>
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th></th>
            </tr>

            @foreach (var restaurant in Model)
            {
                <tr>
                    <td>@restaurant.Id</td>
                    <td>@restaurant.Name</td>
                    <td>
                        <a asp-controller="Restaurant" asp-action="Change" asp-route-Id="@restaurant.Id" class="btn btn-success">Edit</a>
                        <a asp-controller="Restaurant" asp-action="Delete" asp-route-Id="@restaurant.Id" class="btn btn-danger">Delete</a>
                    </td>
                </tr>
            }
            </thead>
        </table>
    </div>
</div>